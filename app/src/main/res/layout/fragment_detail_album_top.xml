<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             tools:context=".ui.DetailAlbumTopFragment">

    <!-- TODO: Update blank fragment layout -->

    <ImageButton
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:scaleType="fitXY"
        android:id="@+id/album_artwork_button"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="20dp"
        android:layout_marginTop="10dp"
        android:onClick="artworkImageButtonClicked"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Album Title"
        android:id="@+id/d_album_name_text"
        android:layout_alignTop="@+id/album_artwork_button"
        android:layout_toEndOf="@+id/album_artwork_button"
        android:textStyle="bold"
        android:textSize="24dp"
        android:paddingStart="10dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Artist Name"
        android:id="@+id/d_artist_text"
        android:layout_below="@+id/d_album_name_text"
        android:layout_alignStart="@+id/d_album_name_text"
        android:paddingStart="10dp"/>
</RelativeLayout>
